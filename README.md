# ConcertHub - Сайт продажи билетов на концерты

Полноценный веб-сайт для продажи билетов на концерты с админ-панелью и базой данных.

## 🚀 Возможности

### Пользовательская часть:
- 🎵 Каталог концертов с фильтрами (жанр, дата, цена)
- 🔍 Поиск по исполнителям
- 🎫 Выбор категорий билетов (Стандарт, VIP, Premium)
- 🛒 Корзина покупок
- 📱 Адаптивный дизайн

### Админ-панель:
- 🔐 Авторизация
- ➕ Добавление/редактирование/удаление концертов
- 📊 Статистика: количество концертов, заказов, выручка
- 📋 Просмотр всех заказов
- 💾 База данных SQLite

## 📦 Установка

### Требования:
- Node.js (версия 14 или выше)
- npm или yarn

### Шаг 1: Установка зависимостей

\`\`\`bash
npm install
\`\`\`

### Шаг 2: Запуск сервера

\`\`\`bash
npm start
\`\`\`

Сервер запустится на http://localhost:3000

### Для разработки (с автоперезагрузкой):

\`\`\`bash
npm run dev
\`\`\`

## 🌐 Структура проекта

\`\`\`
concerthub/
├── server.js              # Backend сервер (Express + SQLite)
├── package.json           # Зависимости
├── concerthub.db         # База данных SQLite (создается автоматически)
└── public/               # Frontend файлы
    ├── index.html        # Основной сайт
    └── admin.html        # Админ-панель
\`\`\`

## 🔑 Доступ к админ-панели

**URL:** http://localhost:3000/admin

**Данные для входа:**
- Логин: `admin`
- Пароль: `admin123`

⚠️ **Важно:** Измените пароль администратора для production!

## 🚀 Деплой на REG.RU (или другой хостинг)

### Вариант 1: Node.js хостинг (рекомендуется)

Если REG.RU поддерживает Node.js хостинг:

1. Загрузите все файлы на сервер через FTP
2. Подключитесь по SSH
3. Установите зависимости:
   \`\`\`bash
   npm install
   \`\`\`
4. Запустите сервер:
   \`\`\`bash
   npm start
   \`\`\`

### Вариант 2: VPS сервер

1. Подключитесь к серверу по SSH
2. Установите Node.js:
   \`\`\`bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   \`\`\`

3. Загрузите проект и установите зависимости:
   \`\`\`bash
   cd /var/www/tales-values.com
   npm install
   \`\`\`

4. Настройте PM2 для автозапуска:
   \`\`\`bash
   sudo npm install -g pm2
   pm2 start server.js --name concerthub
   pm2 startup
   pm2 save
   \`\`\`

5. Настройте Nginx как reverse proxy:
   \`\`\`nginx
   server {
       listen 80;
       server_name tales-values.com;

       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   \`\`\`

### Вариант 3: Использование облачных платформ

**Heroku:**
\`\`\`bash
heroku create concerthub
git push heroku main
\`\`\`

**Railway.app:**
1. Подключите GitHub репозиторий
2. Railway автоматически определит Node.js проект
3. Готово!

**Render.com:**
1. Создайте новый Web Service
2. Подключите репозиторий
3. Build Command: `npm install`
4. Start Command: `npm start`

## 🔧 API Endpoints

### Публичные (без авторизации):
- `GET /api/concerts` - Получить все концерты
- `GET /api/concerts/:id` - Получить один концерт
- `POST /api/orders` - Создать заказ

### Защищенные (требуется авторизация):
- `POST /api/auth/login` - Авторизация
- `POST /api/concerts` - Добавить концерт
- `PUT /api/concerts/:id` - Обновить концерт
- `DELETE /api/concerts/:id` - Удалить концерт
- `GET /api/orders` - Получить все заказы
- `GET /api/stats` - Получить статистику

## 🔒 Безопасность

Для production окружения:

1. **Измените JWT секрет:**
   \`\`\`javascript
   // В server.js или через переменные окружения
   const JWT_SECRET = 'ваш-сложный-секретный-ключ';
   \`\`\`

2. **Создайте новый пароль администратора:**
   Используйте bcrypt для хеширования пароля

3. **Используйте HTTPS:**
   Настройте SSL сертификат (Let's Encrypt бесплатный)

4. **Ограничьте CORS:**
   \`\`\`javascript
   app.use(cors({
       origin: 'https://tales-values.com'
   }));
   \`\`\`

## 📝 Переменные окружения

Создайте файл `.env`:

\`\`\`env
PORT=3000
JWT_SECRET=your-secret-key-change-in-production
NODE_ENV=production
\`\`\`

## 🐛 Troubleshooting

### База данных не создается
- Проверьте права на запись в папку проекта
- Убедитесь, что SQLite установлен

### Ошибка CORS
- Убедитесь, что frontend и backend на одном домене
- Или настройте CORS правильно

### Не работает авторизация
- Проверьте JWT_SECRET
- Убедитесь, что токен передается в заголовке Authorization

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи сервера (`console.log` в server.js)
2. Консоль браузера (F12)
3. База данных создана и содержит данные

## 📄 Лицензия

ISC

---

Сделано с ❤️ для tales-values.com
